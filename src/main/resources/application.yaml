spring:
  application:
    name: examenT2
  datasource:
    url: jdbc:mysql://localhost:3306/viajes_db?useSSL=false&serverTimezone=UTC
    username: root
    password: tu_password_aqui
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true